{
  "name": "lattice",
  "version": "0.1.0",
  "description": "The Dockerfile for AI coding workflows. One config to share your entire setup.",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "lattice": "src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist --target node",
    "lattice": "bun src/cli.ts"
  },
  "dependencies": {
    "yaml": "^2.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.0.0",
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "ai-coding",
    "workflow",
    "configuration"
  ],
  "author": "dpshade",
  "license": "MIT"
}
